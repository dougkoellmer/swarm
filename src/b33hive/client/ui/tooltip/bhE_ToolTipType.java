package b33hive.client.ui.tooltip;

/**
 * The order of this enum maps to the tool tips precedence, which means for now just the z-order...last is highest precedence.
 */
public enum bhE_ToolTipType
{
	STATUS,
	FOCUS,
	TUTORIAL,
	MOUSE_OVER,
	NOTIFICATION;
}
